model:
  base_model_id: microsoft/Phi-3-mini-4k-instruct
  adapter_path: /kaggle/input/datasets/hollychen12345/phi3-skill-lora-adapter/phi3_skill_lora_adapter

data:
  test_path: /kaggle/input/datasets/hollychen12345/test-xls/test.xls

generation:
  max_input_length: 512
  max_new_tokens: 6
  batch_size: 2

evaluation:
  sample_size: 20
  random_seed: 42
  run_baseline: true

output:
  directory: outputs
